integer a.
boolean bool.

void testIfStmt[] [
    if (TRUE) {
        disp <- "Inside if statement 1".
    }
    disp <- "End of if stmt 1\n".

    if (FALSE) {
        disp <- "BAD!".
    }
]

void main[] [
    integer c.
    integer q.
    c = q = 5.
    disp <- c.
    input -> c.
    testIfStmt().
    if (TRUE) {
        disp <- "You wrote ".
        disp <- c.
        return.
    } else {
        disp <- "False branch\n".
    }
    disp <- "End of if-else stmt".
]