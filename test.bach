integer a.
boolean bool.

void main[] [
    if (FALSE) {
        disp <- "Inside if statement".
    }
    disp <- "End of if stmt\n".

    if (FALSE) {
        disp <- "True branch\n".
    } else {
        disp <- "False branch\n".
    }
]

integer myFunc[] [
    integer a.
    boolean b.
]